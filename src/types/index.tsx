// Projects
export interface Project {
    name: string
    description: string
    tags: string[]
    image: string
    link: string
}

// Certs
export interface Cert {
    name: string
    issuer: string
    image: string
}
